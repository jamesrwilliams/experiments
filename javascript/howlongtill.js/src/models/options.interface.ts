export interface Options {
    elm: string,
    elms?: {
        days?: string,
        hours?: string,
        minutes?: string,
        seconds?: string,
    },
    startDate: string,
    hideOnCompletion: boolean,
}
